[build]
  base    = "."
  command = "npm run build"
  publish = "dist"

# Handle React client-side routing — redirect all paths to index.html
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
